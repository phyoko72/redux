import counterSlice from "./counter";
import store from "./store";

// const unsubscribe = store.subscribe(()=> console.log("Initial Count: ", store.getState() ) )

// store.dispatch(counterSlice.actions.increment(5))

// unsubscribe()


